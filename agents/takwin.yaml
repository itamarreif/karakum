# takwin — agent for AI assistant infrastructure (builds out karakum, dewey,
# palimpsest, and the surrounding tooling). Memory = this scratchpad's repo.
#
# An agent is decoupled from:
#   - the toolchain (selected at invocation: `just claude takwin <slug>` runs on claude)
#   - the project it works on (passed at invocation: `just claude takwin <slug> karakum`)
#
# Memory = the agent's persistent self (skills, scratchpad, master prompt).
# Project (optional, per-session) = the codebase the agent acts on.
#
# Schema (MVP):
#   name              - identifier; used in branch names (<name>/<slug>) and container name
#   memory.path       - local path to the agent's memory git repo
#   memory.repository - canonical remote; preflight verifies local origin matches
#   state.path        - host dir mounted at the container's ~/.claude; the agent's
#                       persistent harness state (history, todos, settings), shared
#                       across all of this agent's sessions and projects.
#
# Secrets are host-wide, not per-agent — see ../secrets.yaml.

name: takwin
memory:
  path: ~/code/ai/.agents
  repository: github.com/itamarreif/takwin
state:
  path: ~/code/ai/.claude
